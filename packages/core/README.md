# packages/core

Regras de dominio puras do DealTrust.

Este pacote nao deve depender de framework, banco, HTTP, filas ou interface. A funcao dele e concentrar regras criticas que precisam ser testadas isoladamente antes de serem usadas pela API, admin, web ou workers.

